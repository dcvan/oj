class Solution {
public:
    // dp
    // int maxSubArray(int A[], int n) {
    //     // Note: The Solution object is instantiated only once and is reused by each test case.
    //     int sum = 0, sumAll = A[0];
    //     for (int i = 0; i < n; i++) {
    //         sum = max(A[i], sum+A[i]);
    //         sumAll = max(sum, sumAll);
    //     }
    //     return sumAll;
    // }
    
    // divide-conquer
    int maxSubArray(int A[], int n) {
        // Note: The Solution object is instantiated only once and is reused by each test case.
        
    }
};
